import React from 'react'
import styled from 'styled-components';

function Footer() {
    return (
        <StFooterDiv>Footer</StFooterDiv>
    )
}

export default Footer

const StFooterDiv = styled.div`
    border-top: 1px solid gray;
`